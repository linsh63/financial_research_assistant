# 个人学习记录

## 数据集收集

领域研报：

* 已有 3 个领域：每个再收集 5 份✅
* 扩充 2 个领域：每个再收集 15 份✅

75 + 36 =111 

政策相关：

* 再收集多 39 份

111 + 39 =150 ✅



## RAGFlow

> **需要修改现有代码，借鉴DeepDoc要很深入，不能流于表面**

### DeepDOC

OCR 包含了两个模块：

* TextRecognizer(文字识别)：给定带文字图片，会识别出文字
* TextDetector(找到文字框)：把文字按区域划分，每个区域用4个点来标定

OCR 的使用示例以及返回的结果如下：

```python
ocr = OCR()
result = ocr(page_img)
# result: [(box, (text, score)), ...]
# box 是四点框坐标，text 是识别文本，score 是识别置信度。

only_boxes = list(ocr.detect(page_img))
text = ocr.recognize(page_img, only_boxes[0][0])
```

### chunk

* 拆分：根据不同文件类型，有不同的处理方式。例如 pdf 文件对应 deepdoc
* 合并：按照顺序，合并成大小均等的 chunk

### 混合召回链路和评测指标体系

已经放入文档当中，让 AI 自行学习。

### 总结

初步复现出来了（比第一版好像好了一点），但具体没细看。

其实也细看不来了，最重要的还是去看效果。明天先做以下事情：

* 如果有精力，稍微细看一下。✅
* 再看看表格这些有没有被正确切出来。✅
* 继续跑后面的实验，注意细节的把握：
  * 切块实验✅
  * 向量索引构建



## 0514

对 src 和 script 中的组织架构进行重构，方便我们后续继续构建整个框架。

src下面是不是没必要再套一层financial_report_rag目录？

清理掉一些已经失效的代码

3.向量索引构建

按照原指导文档的这些内容，完成向量索引构建，跑通baseline并登记结果

### 待办

> 根据画出红框的脚本以及我的具体查看，整体还是可以的   ->  提醒我们要多做验证，防止跑偏
>
> 切块上可能还可以根据 chatchat 再更新一版，但确保更新不带来新的问题！
>
> 额度恢复之前构建一下问题集

* 看看 chatchat 有什么可以参考的：

  * 例如语义断裂，辅助更好的切块，我认为这一点是可取的✅

  * 其他内容：根据上次的返回内容来思考一下✅

  * 教训
    * 要保留换行，不要随便去掉空格和换行✅
    * 表格处理现在已经可以了，可以还原出表格来。但理解上，到时候传入大模型可能需要few-shot来指导模型返回输出。

* 额度恢复之前可以来构建问题集，先去原帖中找到构建的方式，然后进行构建。

* 换成 home-brew 环境，不要再使用本机的环境！✅

* 学习 chatchat 中的混合策略，进行第二周的实践



## 0515

那么，请你编写一个处理这150份pdf的代码，要求层层封装，这份代码依次对150份文件进行分页、切块、编码。这份代码暂时不运行，写好放着即可。



## 0518

当前推荐路线做的事情，就是按照不同任务类型，路由到不同的策略，对吗？

按照当前推荐路线，修改代码，然后评测并修改相关文档

之前两次提交的代码，还没有push上去，给我命令

说一下接下来我们要做什么





我使用 openai api，但是不是使用官方接口，而是采用 OPENAI_BASE_URL = "https://api.vveai.com/v1"。现在，请完成“写回答生成链路”的代码，告诉我怎么通过系统变量或其他方式写入 api key，然后给我运行的命令，我正式来跑通这一步骤。



先修prompt：对于使用证据不稳、数据有误的问题，扩充约束。对于summary出现的问题，主要是风格的对齐，也是修复一下prompt，让它不要随意扩展。



这12条case已经测试完毕了，看看结果，看看能否进一步改进？

另外，刚刚的修改会不会引进了新的错误？

Summary 类型的样本，是否还需要进一步改进评判标准？



双实体我认为可以通过改写问题来解决，调用一次LLM，把问题分解之后再做召回。



另外，也尝试解决一下其他几个compare的badcase

神农集团的pdf能不能单独重新解析一下？
